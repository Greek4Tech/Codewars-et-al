//addLength('apple ban') => ["apple 5", "ban 3"]

function leastLarger (arr, num) {
    let arr = []
    for (let j=0; j<a.length; j++) {
        if (a[j] < a[i]) {
            arr.push(j)
        }
    } return a.indexOf(arr.sort((a,b) => a-b) [0])
}

leastLarger([4,1,3,5,6], 0)